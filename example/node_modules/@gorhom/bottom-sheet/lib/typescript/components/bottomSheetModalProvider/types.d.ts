import type { ReactNode } from 'react';
import type { BottomSheetModalPrivateMethods } from '@gorhom/bottom-sheet/lib/typescript/components/bottomSheetModal';

export interface BottomSheetModalRef {
  key: string;
  ref: {
    current: BottomSheetModalPrivateMethods;
  };
  willUnmount: boolean;
}

export interface BottomSheetModalProviderProps {
  children?: ReactNode;
}
